e: localhost:9001
p: 9003

server:
  port: ${p}

spring:
  profiles:
    active: test
  application:
    name: customer
    version: 1.0.0
  cloud:
    config:
      discovery:
        enabled: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://${e}/eureka/

---

spring:
  profiles: test
  
eureka:
  client:
    registry-fetch-interval-seconds: 5 # default 30s